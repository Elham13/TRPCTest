export enum ERole {
  ADMIN = "Admin",
}

export enum EEmiStatus {
  PENDING = "Pending",
  COMPLETED = "Completed",
}

export enum ESaleType {
  SALE = "Sale",
  EXCHANGE = "Exchange",
}

export enum EPaymentMethod {
  EMI = "EMI",
  CARD = "Card",
  CASH = "Cash",
  UPI = "UPI",
  CHEQUE = "Cheque",
}
